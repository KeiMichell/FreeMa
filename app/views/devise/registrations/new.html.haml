%h2 会員情報登録
= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
  = render "devise/shared/error_messages", resource: resource
  .field
    = f.label :"ニックネーム"
    %br
    = f.text_field :nickname
  .field
    = f.label :"メールアドレス"
    %br
    = f.email_field :email, autofocus: true, autocomplete: "email"
  .field
    = f.label :"パスワード"
    - if @minimum_password_length
      %em
        (#{@minimum_password_length} characters minimum)
    %br
    = f.password_field :password, autocomplete: "new-password", placeholder:"半角英数字7文字以上"
  .field
    = f.label :"パスワード確認"
    %br
    = f.password_field :password_confirmation, autocomplete: "new-password"
  %div
    %p 本人確認
    %p
      安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。
      %br
      他のお客さまに公開されることはありません。
  .field
    = f.label :"お名前（全角）"
    %br
    = f.text_field :last_name, placeholder:"姓"
    = f.text_field :first_name, placeholder:"名"
  .field
    = f.label :"お名前ふりがな（全角）"
    %br
    = f.text_field :last_name_kana, placeholder:"せい"
    = f.text_field :first_name_kana, placeholder:"めい"
  .field
    = f.label :"生年月日"
    %br
    = f.date_select :birthday, use_month_numbers: true,start_year: 1930, end_year: (Time.now.year - 10), default: Date.new(1990, 1, 1)
  .actions
    = f.submit "次へ進む"
= render "devise/shared/links"
